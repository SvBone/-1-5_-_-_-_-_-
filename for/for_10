#include <iostream>
using namespace std;
/*Дано целое число N (> 0). Найти сумму
1 + 1/2 + 1/3 + . . . + 1/N*/
int main()
{
	int n; cin >> n;
	float l = 0;
	for (int i = 1; i < n + 1; i++)
	{
		l += 1 / (float)i;
	}
	cout << l << endl;
	system("pause");
	return 0;
}
