#include <iostream>
#include <cmath>
using namespace std;
/*Найти значение функции y = 3x6 − 6x2 − 7 при данном значении x.*/
int main()
{
	int x; cin >> x;
	int y = 3 * pow(x, 6) - 6 * pow(x, 2) - 7;
	printf_s("y(%d) = %d\n", x, y);
	system("pause");
	return 0;
}
