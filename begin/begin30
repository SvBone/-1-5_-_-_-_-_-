#include <iostream>
using namespace std;
/*Дано значение угла α в радианах (0 < α < 2·pi). Определить значение
этого же угла в градусах, учитывая, что 180◦ = pi радианов. В качестве
значения pi использовать 3.14.*/
int main()
{
	double a; cin >> a;
	cout << "angle: " << a * 180 / 3.14  << endl;
	system("pause");
	return 0;
}
